# cpp-prism

Code Prism backend for **cpp**.

Writes SoT to `~/Library/Caches/code-prism/cpp/<projectKey>/` (not into the user project).

```bash
./bin/cpp-prism --root /path/to/user/project
```

Local path: `~/Documents/Code/code-prism/backends/cpp-prism`
