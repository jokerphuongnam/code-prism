#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)"
export CODE_PRISM_LANG="${CODE_PRISM_LANG:-rust}"
export PRISM_EXTS="${PRISM_EXTS:-rs}"
exec node "$DIR/index.mjs" --lang rust "$@"
