#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)"
export PRISM_EXTS="${PRISM_EXTS:-m,mm,h}"
export CODE_PRISM_LANG="${CODE_PRISM_LANG:-objc}"
exec node "$DIR/src/build-sot.mjs" --lang "objc" "$@"
