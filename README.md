# objective-c-prism

Code Prism backend for **objc**.

Writes SoT to `~/Library/Caches/code-prism/objc/<projectKey>/` (not into the user project).

```bash
./bin/objective-c-prism --root /path/to/user/project
```

Local path: `~/Documents/Code/code-prism/backends/objective-c-prism`
